import { MakeStudentLeaderReq } from './make-student-leader-req';

describe('MakeStudentLeaderReq', () => {
  it('should create an instance', () => {
    expect(new MakeStudentLeaderReq()).toBeTruthy();
  });
});
